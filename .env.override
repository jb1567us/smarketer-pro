
# Workaround: Override System Env via .env
# The system apparently has a rogue OPENAI_API_KEY set that confuses the extension.
# We set it to empty here to try and force the extension to ignore it, 
# or use a valid key if you have one.

OPENAI_API_KEY=
